# 公共雨伞业务 AI 项目面试讲稿 + 高频追问答案

> 适用目标：AI 应用开发工程师 / Python 后端 / 全栈偏 AI 应用方向。  
> 核心策略：不要先讲「我会 RAG / Agent / LangGraph」，而是先讲公共雨伞借还业务里的真实问题，再讲为什么需要 AI、怎么设计、难点在哪里。  
> 注意：这里是公开知识库版本，只保留项目表达与技术细节，不放联系方式、简历原文等隐私信息。

## 一、面试表达总原则

### 错误讲法

不要这样开场：

> 我之前主要做前端，最近学习了 FastAPI、RAG、Agent、LangGraph、SSE，所以做了一个 AI 业务运营系统。

问题：

- 一上来暴露「转型」痕迹；
- 面试官会觉得理论堆砌、缺少业务落地；
- RAG / Agent / SSE 像学习成果，不像项目方案；
- 没有解释为什么公共雨伞业务需要 AI。

### 正确讲法

应该这样开场：

> 我做的是一个面向公共雨伞借还业务的 AI 运营与客服辅助系统。这个业务的核心链路是用户扫码借伞、支付押金或免押、设备开锁、订单计费、归还识别、异常处理和客服售后。实际运营中会产生大量重复问题，比如用户借伞失败、归还后订单未结束、设备离线、雨伞丢失、活动规则咨询、网点补伞调度等。传统方式主要靠客服和运营人员查后台、翻规则、问技术，处理效率低，也容易漏掉上下文。所以我设计了一套基于 LLM + RAG + Agent Workflow 的智能运营系统，把业务知识检索、订单 / 设备查询、异常原因分析和处理建议整合到一个可控的 AI 流程里。

这段更有杀伤力，因为它先回答了：

1. 业务是什么；
2. 痛点是什么；
3. AI 解决什么；
4. 技术为什么合理。

---

## 二、整体项目自述

### 30 秒版本

我做的是一个面向公共雨伞借还业务的 AI 运营与客服辅助系统。公共雨伞业务链路比较长，涉及扫码借伞、设备开锁、订单计费、归还识别、押金 / 支付、异常售后和运营调度。实际工作里，客服和运营经常要处理借伞失败、归还未结束、设备离线、订单异常、活动规则咨询等问题。

所以我把 AI 能力落在这些业务问题上：通过 RAG 检索业务规则和处理 SOP，通过 Agent 调用订单、设备、网点等查询工具，再由 LLM 生成面向客服或运营人员的原因分析和处理建议。系统后端用 FastAPI，工作流用 LangGraph，结构化输出用 Pydantic，前端通过 SSE 实时展示模型分析过程和工具执行状态。

另外，公司内部也在基于 OpenClaw 探索数字人和研发提效能力。比如“小龙虾数字人”这个方向，本质不是只做一个会说话的形象，而是把 OpenClaw 的 Agent Loop、工具调用、多 Agent 协作、上下文记忆和多模态交互能力，迁移到公共雨伞客服、运营培训和代码开发提效场景里。对业务侧，它可以变成公共雨伞数字人客服 / 运营助手；对研发侧，它可以作为代码理解、接口联调、问题排查和文档生成的 AI 工程助手。

### 2 分钟版本

我这个项目不是单纯做一个聊天机器人，而是围绕公共雨伞借还业务做的 AI 运营与客服辅助系统。

公共雨伞业务看起来简单，其实链路比较长：用户扫码后要判断用户状态、押金或免押资格、设备在线状态、伞柜锁控状态；借出后要创建订单、开始计费；归还时要识别归还网点、更新订单状态；如果中间出现设备离线、锁控失败、用户归还后订单没结束、活动规则不一致，就会产生大量客服和运营问题。

传统处理方式是客服先看用户描述，再去后台查订单、查设备、查网点、翻活动规则或 SOP，复杂问题还要找技术排查。这种方式有三个问题：

1. **处理效率低**：一个问题可能要跨多个后台页面查询；
2. **规则容易不一致**：不同客服对押金、计费、异常退款、活动规则理解不一样；
3. **问题定位依赖经验**：比如借伞失败到底是设备离线、锁控异常、用户欠费，还是网点无伞，需要根据多源信息判断。

所以我设计了一套 AI 运营辅助流程：

1. 用户或客服输入问题，例如“用户说伞已经还了但订单还在计费”；
2. 系统先做意图识别，判断是订单异常、设备异常、规则咨询还是运营调度；
3. RAG 检索相关业务规则、售后 SOP、活动说明和异常处理文档；
4. Agent 根据意图选择工具，比如查询订单状态、设备状态、网点库存、用户历史记录；
5. LLM 结合检索资料和工具结果，生成原因分析、处理步骤、需要人工确认的风险点；
6. 通过 SSE 把“正在查询订单 / 正在检索规则 / 正在生成建议”等过程实时返回。

技术上，FastAPI 负责后端接口，Pydantic 约束请求参数、工具参数和模型结构化输出，LangGraph 管理多步骤状态流转，RAG 负责业务知识检索，SSE 负责流式展示。

这个项目的核心价值是：把 AI 从“泛泛问答”变成“能结合公共雨伞订单、设备、网点和规则的业务处理助手”。

---

## 三、项目一：公共雨伞 AI 运营与客服辅助系统

### 1. 面试官问：介绍一下这个项目

#### 回答

这是一个面向公共雨伞借还业务的 AI 运营与客服辅助系统，主要服务于客服、运营和内部处理人员。

公共雨伞业务里经常出现几类问题：

- 用户扫码借伞失败；
- 用户归还后订单没有结束，还在计费；
- 设备离线或锁控异常；
- 用户咨询押金、扣费、优惠活动规则；
- 某个网点无伞、满柜或需要补伞；
- 客服需要根据订单和设备状态判断是否退款、是否转人工或是否派运营处理。

这些问题的共同点是：它们不是单纯靠模型常识能回答的，必须结合业务规则、订单数据、设备状态和售后 SOP。

所以系统设计上，我把它分为四块：

1. **业务知识库**：存放借还规则、计费规则、押金规则、异常处理 SOP、活动说明等，通过 RAG 检索。
2. **业务工具调用**：把订单查询、设备查询、网点库存查询、用户状态查询封装成受控工具。
3. **Agent 工作流**：根据问题类型决定先检索规则，还是先查订单 / 设备，再生成处理建议。
4. **流式交互**：通过 SSE 展示处理过程，让客服知道系统正在做什么，而不是一直等待。

最终输出不是一句泛泛回答，而是结构化结果：问题类型、可能原因、证据来源、处理步骤、是否需要人工确认、建议回复话术。

### 2. 面试官问：这个项目的业务难点是什么？

我认为业务难点主要有四个。

第一，**借还链路长，异常原因多**。比如“借伞失败”可能是用户欠费、押金不足、设备离线、锁控失败、柜中无伞、活动资格不满足，也可能是第三方支付或小程序链路异常。如果 AI 只回答通用话术，没有结合订单和设备状态，就没有实际价值。

第二，**订单状态和设备状态需要联合判断**。比如用户说“我已经还伞了，但订单还在计费”，不能只看订单状态。还要看归还网点、设备上报记录、伞锁状态、最后一次心跳时间、是否存在网络延迟或设备离线。这个场景适合用 Agent 调用多个工具，再综合判断。

第三，**业务规则经常变化**。比如押金规则、优惠活动、免费时长、超时计费、异常退款政策可能随着运营策略调整。如果把规则写死在 Prompt 里，很容易过期，所以要用 RAG 从最新 SOP 和活动文档里检索。

第四，**客服回复要稳定、可解释**。客服不能拿一个不确定的模型回答直接回复用户，所以输出里要有证据来源、置信度、是否需要人工复核和推荐处理步骤。

### 3. 面试官问：项目亮点是什么？

我会讲三个亮点。

#### 亮点一：不是泛问答，而是业务状态驱动的 Agent

很多 AI 客服只能回答 FAQ，但这个项目会根据用户问题判断是否需要查业务状态。

比如用户问：

> 我明明还伞了，为什么还在扣费？

系统不会直接回答“请联系客服”，而是走一个 Agent 流程：

1. 识别意图为“归还异常 / 订单未结束”；
2. 调用订单查询工具，看订单是否仍在进行中；
3. 调用设备或网点工具，看归还点是否在线、是否有归还上报；
4. 检索 SOP，确认这种异常对应的处理规则；
5. 输出可能原因：设备离线导致归还事件延迟、归还未被识别、用户还到非指定点等；
6. 给客服处理建议：是否先安抚用户、是否需要冻结计费、是否转运营核查。

这个亮点体现的是：AI 和业务系统结合，不是只会聊天。

#### 亮点二：RAG 知识库按业务场景组织，而不是简单文档切片

我不会只把所有文档丢进向量库，而是按公共雨伞业务场景组织 Metadata：

- `borrow`：借伞规则、借伞失败处理；
- `return`：归还规则、订单未结束处理；
- `payment`：押金、扣费、退款、免押规则；
- `device`：设备离线、锁控失败、心跳异常；
- `station`：网点库存、满柜、缺伞、运营补伞；
- `activity`：优惠活动、免费时长、券规则；
- `after_sales`：客服 SOP、补偿规则、转人工条件。

检索时先做意图分类，再按业务类型过滤，避免“用户问归还异常，却召回活动规则”这种问题。

#### 亮点三：输出结构化，方便客服直接使用

最终回答不是一段散文，而是结构化输出，例如：

```json
{
  "intent": "return_order_not_closed",
  "risk_level": "medium",
  "possible_reasons": ["设备离线导致归还事件未及时上报", "订单状态未同步"],
  "evidence": ["订单当前状态：租借中", "归还网点最近心跳：10分钟前"],
  "suggested_actions": ["先告知用户正在核查", "查询设备归还记录", "必要时冻结异常计费"],
  "need_human_review": true,
  "reply_template": "您好，我们已看到您的订单仍在计费，会先为您核查归还记录，请您稍等。"
}
```

这样客服可以直接判断怎么处理，系统也能基于字段做后续流转。

### 4. 面试官问：RAG 在这个业务里怎么设计？

我的设计不是泛泛地“文档切片 + 向量检索”，而是围绕公共雨伞业务场景来做。

第一步是整理知识源：

- 借伞 / 还伞流程说明；
- 计费、押金、免押、退款规则；
- 设备异常处理 SOP；
- 订单异常处理 SOP；
- 活动规则、优惠券规则；
- 客服标准话术；
- 网点运营规则。

第二步是按业务语义切分。比如“归还后订单未结束”这类 SOP，要保留完整处理步骤，不能机械按固定长度切断，否则模型召回后会缺步骤。

第三步是给 chunk 加 Metadata，包括业务类型、适用场景、规则版本、是否客服可见、是否需要人工确认。

第四步是检索策略：

1. 先用 LLM 或规则做意图识别；
2. 根据意图加 Metadata 过滤；
3. 再做向量检索；
4. 必要时结合关键词，比如订单、押金、归还、离线、退款；
5. 最后把召回内容按 SOP 优先级拼进 Prompt。

这样可以提高召回准确率，也能避免模型拿错业务规则。

### 5. 面试官问：Agent 在这个业务里怎么工作？

我会用“订单未结束”举例。

用户输入：

> 我已经把伞还了，为什么订单还在计费？

Agent 流程大概是：

1. **意图识别节点**：判断为 `return_order_not_closed`。
2. **参数抽取节点**：抽取订单号、用户 ID、网点信息；如果缺少订单号，就返回需要补充的信息。
3. **订单查询工具**：查询订单当前状态、开始时间、计费状态。
4. **设备 / 网点查询工具**：查询归还网点设备在线状态、最近心跳、是否有归还记录。
5. **RAG 检索节点**：检索“归还后订单未结束”的 SOP。
6. **分析节点**：综合订单、设备、SOP，判断可能原因。
7. **结构化输出节点**：输出原因、证据、建议动作、客服话术。

这里 Agent 的价值是动态决策：有些问题只需要 RAG，有些必须查订单，有些还要查设备。不同意图走不同路径，比固定 Chain 更适合。

### 6. 面试官问：为什么用 LangGraph？

因为公共雨伞异常处理不是固定的一条链路，而是状态驱动的多分支流程。

比如：

- 活动规则咨询：只需要 RAG + 回复生成；
- 借伞失败：需要查用户状态、设备状态、网点库存；
- 归还未结束：需要查订单、设备上报、归还 SOP；
- 退款争议：需要查订单、支付状态、退款规则，并标记人工复核。

LangGraph 可以把这些流程拆成节点：意图识别、参数抽取、工具调用、RAG 检索、结果分析、人工复核判断、回复生成。每个节点输入输出明确，也方便做异常边、重试边和兜底边。

### 7. 面试官问：这个项目怎么处理模型输出不稳定？

公共雨伞业务里，模型输出不稳定会直接影响客服判断，所以我主要做三层控制。

第一层是 **结构化输出**。用 Pydantic 定义字段，比如 `intent`、`possible_reasons`、`evidence`、`suggested_actions`、`need_human_review`、`reply_template`。模型必须按结构返回。

第二层是 **业务校验**。比如风险等级只能是 low / medium / high；处理动作必须来自允许列表；如果涉及退款、冻结计费、订单关闭，必须标记人工复核，不能让模型直接决策。

第三层是 **兜底策略**。如果模型输出解析失败、证据不足或工具调用失败，就返回“需要人工核查”，同时给出已查到的信息，而不是编造答案。

### 8. 面试官问：你怎么避免 AI 乱操作订单？

核心原则是：模型不能直接操作订单，所有副作用动作必须经过后端权限和人工确认。

系统里工具分两类：

- **只读工具**：查询订单、设备、网点、用户状态，AI 可以在权限范围内调用；
- **写操作工具**：退款、关闭订单、冻结计费、派单处理，这类只能生成建议，不能自动执行，或者必须经过人工确认。

即使后续支持自动化，也要有：

- 用户身份和角色权限；
- 工具白名单；
- 参数校验；
- 幂等控制；
- 操作审计日志；
- 高风险动作二次确认。

这能体现我对 AI 工程安全边界的理解。

### 9. 面试官问：SSE 在这个项目里的价值是什么？

客服处理异常时，如果接口要等 10 秒才返回完整结果，体验会很差，也不知道系统卡在哪里。SSE 可以把处理过程分阶段推出来：

- `intent_detected`：已识别为归还异常；
- `tool_start`：正在查询订单；
- `tool_result`：订单状态为租借中；
- `rag_search`：正在检索归还异常 SOP；
- `analysis`：正在生成处理建议；
- `done`：输出最终建议。

这样客服会感觉系统在持续工作，也方便定位是哪一步慢，比如订单接口慢、设备接口慢还是模型生成慢。

### 10. 面试官问：你怎么评估这个项目效果？

我会从业务和 AI 两个维度评估。

业务维度：

- 客服平均处理时长是否下降；
- 常见问题一次解决率是否提升；
- 订单异常定位时间是否缩短；
- 是否减少客服反复询问技术或运营；
- 高风险问题是否能正确转人工。

AI 维度：

- 意图识别准确率；
- RAG 召回命中率；
- 工具调用成功率；
- 结构化输出解析成功率；
- 幻觉率或无依据回答比例；
- Agent 平均执行步数和失败原因。

这类项目不能只看模型回答好不好，还要看它有没有真正降低业务处理成本。

---

## 四、项目二：基于 OpenClaw 的公共雨伞后台自动化与智能数字人系统

### 1. 面试官问：介绍一下这个 OpenClaw 项目

这个项目我会定位为 **基于 OpenClaw 的公共雨伞后台自动化与智能数字人系统**。

公司内部在做 OpenClaw 和数字人方向的探索，比如“小龙虾数字人”这类样板项目，主要验证数字人形象、语音交互、多轮对话和 Agent 工具调用能力。但真正落到公共雨伞业务里，我认为不能只停留在“会说话的数字人”，而是要把 OpenClaw 接入后台系统，让它能调用后台能力，自动完成客服、运营和研发中的重复流程。

我的设计思路是：

1. 后台系统把订单、设备、网点、用户、活动、售后等能力封装成 CLI 或标准 Tool；
2. OpenClaw 把这些 CLI / Tool 注册成可控工具；
3. Agent 根据用户或运营人员的任务，自动选择工具、查询数据、分析原因、生成处理建议；
4. 前端再做一个操作界面，对接 OpenClaw 的会话、事件流和工具执行状态；
5. 数字人 / 语音客服可以作为更自然的交互入口，但底层核心是后台业务自动化。

所以这个项目不是单纯数字人项目，而是 **OpenClaw + 后台 CLI + Agent Workflow + 前端自动化界面** 的业务自动化平台。

### 2. 面试官问：为什么要把后台系统能力 CLI 化 / Tool 化？

公共雨伞后台有很多高频操作，本质上都是固定流程，但人工操作成本高。

比如：

- 客服处理“还伞后仍扣费”，需要查订单、查设备、查归还记录、查 SOP；
- 运营排查某个网点异常，需要查设备在线状态、库存、最近借还记录；
- 活动配置上线前，需要检查优惠券规则、适用城市、时间范围、冲突活动；
- 技术排查线上问题，需要查日志、查接口返回、查订单状态流转；
- 新人了解业务，需要翻后台、看接口文档、问老同事。

如果这些能力只存在于后台页面里，AI 很难稳定调用；如果直接让模型操作数据库，又不安全。所以比较合理的方式是把后台能力封装成 CLI 或 Tool：

```bash
umbrella order get --order-id xxx
umbrella device status --device-id xxx
umbrella station inventory --station-id xxx
umbrella policy search --intent return_order_not_closed
umbrella ops abnormal-orders --date 2026-07-20
```

这样 OpenClaw 调用的是受控命令，而不是随便拼 SQL 或模拟人工乱点页面。

### 3. 面试官问：前端界面怎么对接 OpenClaw？

前端可以做成一个“AI 后台助手”界面，而不是传统表单堆功能。

界面上可以有几类入口：

1. **自然语言任务输入**：运营输入“帮我排查这个订单为什么还在计费”；
2. **场景化快捷按钮**：比如“归还异常排查”“设备离线诊断”“网点缺伞分析”“活动配置检查”；
3. **工具执行时间线**：展示 OpenClaw 正在调用哪些工具，比如查订单、查设备、检索 SOP；
4. **结构化结果面板**：展示原因分析、证据、建议动作、风险等级、是否需要人工确认；
5. **人工确认区**：涉及退款、关闭订单、冻结计费、批量操作时，必须由人确认后执行；
6. **执行记录和审计**：记录谁发起、Agent 做了什么、调用了哪些工具、最终执行了什么。

这样前端不只是聊天框，而是一个可观察、可确认、可追溯的自动化操作台。

### 4. 面试官问：这个项目的技术架构怎么设计？

我会按七层讲。

第一层是 **前端自动化操作台**：提供自然语言输入、场景按钮、执行时间线、结果面板和人工确认。

第二层是 **OpenClaw Agent Runtime**：负责 Agent Loop、工具选择、事件流、上下文管理、多 Agent 协作和权限策略。

第三层是 **后台 CLI / Tool 层**：把订单、设备、网点、用户、活动、售后、日志、代码仓库等能力封装成标准命令或工具。

第四层是 **业务系统 API 层**：CLI 最终调用真实后端接口或内部服务，而不是直接让模型操作数据库。

第五层是 **RAG 知识库层**：存放借还规则、计费规则、押金退款规则、设备异常 SOP、活动规则、客服话术、接口文档和研发排查文档。

第六层是 **安全与审批层**：工具白名单、参数校验、角色权限、dry-run 预览、人工确认、幂等控制、审计日志。

第七层是 **数字人 / 语音交互层**：作为可选入口，把文本、语音、数字人形象接入同一套 OpenClaw 自动化能力。

### 5. 面试官问：项目亮点是什么？

我会讲四个亮点。

#### 亮点一：把后台功能从“人点页面”升级成“Agent 调工具”

传统后台是人根据经验点菜单、查页面、复制数据、再判断下一步。这个项目把后台能力拆成可调用工具，让 OpenClaw 根据任务自动编排。

比如“归还后仍扣费”场景，Agent 可以自动执行：

1. 查订单状态；
2. 查设备心跳；
3. 查网点归还记录；
4. 检索归还异常 SOP；
5. 汇总证据；
6. 给出客服话术和处理建议。

这比单纯 AI 问答更进一步，因为它能真正操作后台能力。

#### 亮点二：前端界面不是聊天框，而是自动化操作台

我不会只做一个 Chat UI，而是把 Agent 的执行过程可视化。

前端会展示：

- 当前识别出的业务意图；
- 正在调用的工具；
- 每个工具的返回摘要；
- 最终原因分析；
- 建议动作；
- 高风险动作确认按钮；
- 操作审计记录。

这样业务人员能看懂 Agent 为什么这么判断，也能在关键动作前介入。

#### 亮点三：业务自动化和研发提效共用同一套工具体系

同一套 OpenClaw 工具体系，既可以服务运营，也可以服务研发。

业务侧工具：订单查询、设备查询、网点库存、用户状态、活动规则、售后 SOP。

研发侧工具：代码搜索、接口文档查询、日志分析、测试命令、Mock 生成、问题复盘文档生成。

这说明项目不是一个孤立 Demo，而是一套可扩展的 Agent 平台化能力。

#### 亮点四：高风险后台操作有安全边界

后台系统有很多敏感动作，比如退款、关闭订单、冻结计费、批量派单、修改活动配置。我的设计是：

- 默认先 dry-run，展示将要执行的动作；
- Agent 只能生成建议，不能直接越权执行；
- 写操作必须经过角色权限和人工确认；
- 每次操作都有审计日志；
- 关键操作要有幂等键，避免重复执行；
- 支持失败回滚或人工补偿。

这能体现 AI 自动化不是盲目放权，而是可控自动化。

### 6. 面试官问：项目难点是什么？

难点主要有五个。

第一，**后台能力怎么抽象成稳定工具**。后台页面面向人，工具面向 Agent。工具参数、返回结构、错误码、权限边界都要重新设计。

第二，**Agent 如何避免误操作**。模型可能误判意图或参数，所以必须做参数校验、业务校验、dry-run、人审和审计。

第三，**长流程自动化的状态管理**。一个任务可能涉及多个工具调用，比如查订单、查设备、查 SOP、生成建议、等待确认、执行动作。需要用事件流和状态机管理过程。

第四，**业务规则和实时数据融合**。RAG 负责规则，CLI / Tool 负责实时状态，最终回答必须把两者结合起来。

第五，**前端可解释性**。如果 Agent 在后台默默执行，业务人员不敢用。所以前端必须把工具调用过程、证据和风险点展示清楚。

### 7. 面试官问：举一个完整业务流程例子

以“用户还伞后订单仍在计费”为例。

运营人员在前端输入：

> 帮我排查订单 123 为什么还在计费。

OpenClaw 的执行流程：

1. 识别意图：归还异常 / 订单未结束；
2. 调用 CLI：`umbrella order get --order-id 123`；
3. 调用 CLI：`umbrella device status --device-id D001`；
4. 调用 CLI：`umbrella station return-records --station-id S001`；
5. RAG 检索“归还后订单未结束”的 SOP；
6. 判断可能原因：设备离线、归还事件未上报、订单同步失败、用户还错网点；
7. 生成结构化结果：原因、证据、建议动作、用户话术；
8. 如果建议冻结计费或退款，进入人工确认；
9. 确认后调用受控写工具执行；
10. 记录审计日志和处理结果。

这个流程体现的是：AI 不是直接替客服说话，而是把后台排查流程自动化。

### 8. 面试官问：研发提效怎么落地？

研发提效侧，我会围绕公共雨伞后台系统的开发流程讲。

OpenClaw 接入代码仓库、接口文档、CLI 工具和日志后，可以做：

- **理解业务代码**：解释订单状态、设备状态、支付押金、活动配置等模块；
- **生成 CLI / Tool 封装**：根据后台接口生成工具 schema、参数校验和调用示例；
- **辅助接口联调**：生成 Mock 数据、测试请求、异常状态用例；
- **排查线上问题**：根据日志和订单号定位可能的代码分支；
- **生成自动化脚本**：把重复后台操作沉淀成 CLI；
- **沉淀知识库**：把问题排查过程、接口约定、SOP 更新到知识库。

这样开发代码提效不只是“让 AI 写代码”，而是围绕真实后台业务，把理解、联调、排查、文档和自动化脚本都串起来。

### 9. 面试官问：数字人和语音客服在这个项目里是什么位置？

数字人和语音客服是交互入口，不是单独项目。

底层能力是 OpenClaw 接入后台工具和知识库；上层可以有多种入口：

- 后台操作台：给客服和运营使用；
- 数字人入口：给用户或培训场景使用；
- 语音客服入口：给移动端或用户咨询使用；
- 研发助手入口：给开发人员使用。

所以我不会把“实时语音客服”单独拆成一个项目，而是把它作为 OpenClaw 自动化平台的一个交互形态。这样能避免项目重复，也更符合真实系统演进。

### 10. 面试官问：怎么评估这个项目效果？

业务侧指标：

- 异常订单平均排查时长是否下降；
- 客服 / 运营跨页面查询次数是否减少；
- 常见问题自动化处理率；
- 高风险操作拦截准确率；
- SOP 命中率和处理建议采纳率；
- 操作审计完整率。

研发侧指标：

- CLI / Tool 覆盖的后台能力数量；
- 接口联调问题定位时间；
- Mock / 测试用例生成效率；
- 线上问题复盘沉淀数量；
- 新人理解业务模块耗时；
- Agent 建议代码或脚本的采纳率。

### 11. 项目一句话总结

一句话可以这样说：

> 这个项目是把 OpenClaw 接入公共雨伞后台系统，将订单、设备、网点、售后、活动等后台能力 CLI / Tool 化，再通过前端自动化操作台和数字人入口触发 Agent 工作流，实现客服运营流程自动化和研发代码提效。

---

## 五、项目三：公共雨伞业务系统与 AI 落地基础


### 1. 面试官问：你之前的业务系统经验怎么支撑 AI 项目？

我会把这段经历讲成 AI 项目的业务基础，而不是单独讲前端。

公共雨伞业务系统覆盖扫码借伞、订单计费、归还识别、设备管理、网点运营、活动配置和客服售后。做 AI 项目前，我已经理解这套业务链路，所以我知道 AI 不能只做通用问答，而要和订单、设备、用户、网点、活动规则结合。

比如：

- 用户借不了伞，要查用户资格、设备状态、网点库存；
- 用户还伞后仍计费，要查订单状态、设备上报和归还规则；
- 用户问押金退款，要查支付状态、押金规则和退款 SOP；
- 运营问哪个网点缺伞，要结合网点库存和历史借还数据。

这段业务经验让我在设计 AI 应用时，能自然地把问题拆成“业务意图 + 数据查询 + 规则检索 + 处理建议”，而不是停留在理论层面。

### 2. 面试官问：公共雨伞业务里有哪些适合 AI 的场景？

我会分四类。

第一类是 **客服问答**：借还规则、押金、计费、活动、退款等问题，可以用 RAG + 话术生成。

第二类是 **异常诊断**：借伞失败、归还未结束、设备离线、订单异常，可以用 Agent 调用订单 / 设备 / 网点工具做综合判断。

第三类是 **运营辅助**：网点缺伞、满柜、设备异常聚合、补伞优先级建议，可以结合数据分析和 LLM 生成运营建议。

第四类是 **知识库维护**：把客服 SOP、活动规则、设备异常文档整理成结构化知识，辅助新客服培训和内部查询。

### 3. 面试官问：你怎么把业务系统抽象成 AI 工具？

我会按业务对象封装工具，而不是直接暴露数据库。

例如：

- `get_order_status(order_id)`：查询订单状态、计费状态、借还时间；
- `get_device_status(device_id)`：查询设备在线状态、心跳时间、锁控状态；
- `get_station_inventory(station_id)`：查询网点可借 / 可还数量；
- `get_user_risk_status(user_id)`：查询用户是否欠费、是否有未完成订单；
- `search_policy(intent)`：检索对应业务 SOP。

模型只负责选择工具和生成参数，真正的权限校验、SQL 查询、结果脱敏和异常处理都在后端完成。

### 4. 面试官问：这个业务里最能体现工程能力的点是什么？

我会说是“多源状态联合判断 + 可控自动化”。

公共雨伞异常往往不是单一数据能判断的，比如订单未结束可能涉及订单状态、设备上报、网点归还记录、用户描述和 SOP 规则。AI 的价值是把这些信息汇总起来，但工程上必须保证：

- 查询工具可靠；
- 参数和权限可校验；
- 模型不能越权执行；
- 结果要有证据；
- 高风险处理要人工复核；
- 失败时要能兜底。

这比单纯做一个 ChatBot 更接近真实 AI 应用开发。

---

## 六、高频综合追问答案

### 1. 你怎么理解 AI 应用开发？

AI 应用开发不是简单接大模型 API，而是把模型能力嵌入具体业务流程，形成可用、可控、可评估的系统。

以公共雨伞业务为例，AI 应用不是让模型回答“怎么借伞”这么简单，而是要能理解“借伞失败”“归还未结束”“押金退款”“设备离线”这些业务意图，然后结合订单、设备、网点和 SOP 给出处理建议。

所以 AI 应用开发包含：业务建模、RAG 知识库、工具调用、Agent 流程、结构化输出、权限控制、流式交互、日志评估和持续迭代。

### 2. RAG 和业务怎么结合？

RAG 一定要和业务场景结合，不能只是把文档切片丢进向量库。

在公共雨伞业务里，我会按借伞、归还、支付押金、设备异常、网点运营、活动规则、售后 SOP 来组织知识库，并给每个 chunk 加 Metadata。用户问题进来后先识别意图，再用业务类型过滤，最后做向量召回。

这样可以避免召回错场景的规则，比如用户问归还异常，却召回到优惠券活动说明。

### 3. 向量检索不准怎么办？

我会从业务链路排查：

1. 是否先做了意图分类；
2. chunk 是否按完整 SOP 切分，而不是切断步骤；
3. Metadata 是否覆盖 borrow / return / payment / device / station 等场景；
4. 是否需要关键词补充，比如“扣费”“押金”“还伞”“离线”；
5. 是否需要 Rerank；
6. 是否存在过期规则，需要版本字段过滤。

核心是让检索服务业务判断，而不是只追求语义相似。

### 4. Agent 和普通工作流有什么区别？

普通工作流适合固定步骤；Agent 适合根据问题动态选择下一步。

公共雨伞里，不同问题路径不一样：

- 规则咨询：RAG 就够；
- 借伞失败：要查用户、设备、网点；
- 归还未结束：要查订单、设备上报、归还 SOP；
- 退款争议：要查支付状态和退款规则，并进入人工复核。

所以我会用 LangGraph 把 Agent 做成可控的状态机，而不是让模型无限自由调用工具。

### 5. Function Calling / Tool Calling 的核心是什么？

核心是让模型能选择业务工具，但执行权必须在后端。

在公共雨伞项目里，模型可以选择 `get_order_status`、`get_device_status`、`search_policy` 这些工具，但工具参数要经过 Pydantic 校验，用户权限要在后端判断，返回结果要脱敏，高风险动作不能直接执行。

一句话：模型负责决策建议，后端负责安全执行。

### 6. 怎么减少大模型幻觉？

我会结合业务证据控制幻觉：

- Prompt 要求基于订单、设备和 SOP 回答；
- RAG 召回规则必须带来源；
- 工具结果作为事实输入；
- 输出里要包含 evidence 字段；
- 资料不足时必须说无法确认；
- 涉及退款、冻结计费、关闭订单必须转人工确认。

这样模型即使生成自然语言，也不能脱离业务证据乱说。

### 7. 你怎么做权限控制？

权限不能交给模型判断。

公共雨伞业务里，查询订单、查看用户信息、退款、关闭订单、冻结计费都是敏感能力。我的设计是：

- 模型只生成工具调用意图；
- 后端根据当前客服或运营角色判断权限；
- 只读工具和写工具分级；
- 写操作默认人工确认；
- 所有操作记录审计日志；
- 工具返回结果做脱敏。

### 8. 你怎么处理 LLM API 不稳定？

我会做超时、重试、降级和观测。

比如客服正在处理用户订单异常，如果模型接口卡死，不能让客服一直等。所以每次 LLM 请求要设置 timeout；对网络抖动和 5xx 做指数退避；失败时返回兜底建议，例如“当前 AI 分析失败，已展示订单和设备查询结果，请转人工处理”；同时记录耗时、错误类型和重试次数。

### 9. SSE 在业务里解决什么问题？

SSE 解决的是客服等待过程不可见的问题。

公共雨伞异常诊断可能要查订单、查设备、检索 SOP、生成建议。如果一次性返回，客服不知道系统是否卡住。SSE 可以实时推送：正在识别问题、正在查订单、正在查设备、正在检索规则、正在生成建议。

它不只是技术优化，而是提升客服处理体验和可观测性。

### 10. 你怎么评估 AI 项目？

不能只评估模型回答，要评估业务效果。

公共雨伞项目里，我会看：

- 借还异常平均处理时间；
- 客服一次解决率；
- SOP 召回命中率；
- 工具调用成功率；
- 结构化输出解析成功率；
- 高风险问题转人工准确率；
- 用户投诉或重复咨询是否下降。

AI 指标要最终回到业务效率和处理质量。

### 11. 如果面试官质疑：这些 AI 项目是不是包装出来的？

可以这样回答：

> 我理解您的顾虑。我的项目表达重点不是说我做了一个大而全的平台，而是围绕公共雨伞借还业务，把真实业务问题抽象成 AI 应用方案。这里面最关键的不是模型本身，而是我对订单、设备、网点、售后 SOP 这些业务对象的理解，以及怎么用 RAG、工具调用和 Agent 流程把它们串起来。即使在实际落地中是分阶段建设，整体设计也是基于真实业务痛点，而不是脱离业务的 demo。

这段要讲得稳，不要心虚。重点强调“业务真实 + 方案合理 + 分阶段落地”。

### 12. 如果面试官问：你最大的项目亮点是什么？

我的最大亮点是把公共雨伞业务异常处理抽象成了“意图识别 + 多源工具查询 + RAG 规则检索 + 结构化处理建议”的 AI 工作流。

它解决的不是泛泛聊天，而是客服和运营真实会遇到的问题，比如借伞失败、归还未结束、设备离线和押金退款。技术上用 Agent 处理动态路径，用 RAG 保证规则准确，用 Pydantic 保证输出稳定，用权限和人工复核保证安全边界。

### 13. 如果面试官问：OpenClaw 怎么接入后台系统？

我会说，不建议让模型直接操作数据库或模拟点击后台页面，而是把后台能力封装成 CLI / Tool。

比如公共雨伞后台可以提供订单查询、设备状态、网点库存、活动配置检查、售后 SOP 检索等命令。OpenClaw 把这些命令注册成工具，模型只负责选择工具和生成参数；真正的权限校验、参数校验、业务校验、执行和审计都在后端完成。

前端再提供一个自动化操作台，把 Agent 的工具调用过程、结果证据、风险提示和人工确认展示出来。这样既能简化后台工作流，又能保证安全可控。

### 14. 如果面试官问：数字人、语音客服和后台自动化是什么关系？

我会说，数字人和语音客服是交互入口，后台自动化才是核心能力。

底层是 OpenClaw 接入公共雨伞后台工具和知识库；上层可以有文本聊天、语音客服、数字人形象、后台操作台等不同入口。用户看到的是数字人或语音客服，但真正解决问题的是 Agent 调用订单、设备、网点、SOP 等工具完成业务判断。

所以我不会把实时语音客服单独拆成一个项目，而是把它作为 OpenClaw 后台自动化平台的一个交互形态。

### 15. 如果面试官问：OpenClaw 对开发代码提效体现在哪里？

我会结合公司公共雨伞后台系统讲。

研发提效主要体现在五点：

1. **代码理解**：快速解释订单、设备、支付、活动等模块的代码结构和状态流转；
2. **工具封装**：根据后台接口生成 CLI / Tool 的 schema、参数校验和调用示例；
3. **接口联调**：根据接口文档生成 Mock、类型定义、测试请求和异常场景；
4. **问题排查**：结合报错日志、接口返回和业务状态，定位可能的问题分支；
5. **知识沉淀**：把排查过程、接口约定、业务规则沉淀到知识库，减少重复沟通。

OpenClaw 的优势是它不只是聊天，而是可以读代码、查文档、跑命令、调用工具，并把这些动作放在一个可控的 Agent 流程里。

---

## 七、简历项目压缩表达

### 公共雨伞 AI 运营与客服辅助系统

围绕公共雨伞借还业务，构建基于 LLM + RAG + Agent Workflow 的 AI 运营与客服辅助系统，支持借伞失败、归还未结束、押金 / 计费咨询、设备异常、网点运营等场景。负责业务意图识别、RAG 知识库设计、订单 / 设备 / 网点查询工具抽象、Agent 状态流转、Pydantic 结构化输出和 SSE 流式响应设计，实现从用户问题到规则检索、业务查询、原因分析和处理建议生成的完整闭环。

### 基于 OpenClaw 的公共雨伞后台自动化与智能数字人系统

基于 OpenClaw Agent Runtime 设计公共雨伞后台自动化与智能数字人系统，将订单、设备、网点、售后、活动等后台能力封装为 CLI / Tool，并通过前端自动化操作台对接 OpenClaw，实现自然语言触发业务排查、工具调用、SOP 检索、结构化处理建议和人工确认。业务侧支持归还异常、借伞失败、设备离线、网点调度等流程自动化；交互侧支持文本、语音和数字人入口；研发侧支持代码理解、CLI 工具封装、接口 Mock、联调排查、日志分析和知识库沉淀，并通过权限、dry-run、审计和幂等控制高风险操作。

### 公共雨伞业务系统与 AI 落地基础

深入参与公共雨伞业务系统建设，理解扫码借伞、订单计费、归还识别、设备管理、网点运营、活动配置和售后处理等核心链路。基于真实业务场景抽象 AI 应用能力，将订单、设备、网点、用户状态和 SOP 规则封装为可被 Agent 调用的受控工具，为后续 AI 运营辅助和智能客服落地提供业务基础。

---

## 八、背诵口诀

项目主线：

> 公共雨伞借还业务 → 客服 / 运营痛点 → RAG 管规则 → Agent 查状态 → 结构化给建议 → 权限控风险。

OpenClaw 后台自动化主线：

> 后台能力 CLI / Tool 化 → OpenClaw 做 Agent 编排 → 前端操作台展示过程 → 数字人 / 语音作为入口 → 客服运营自动化 + 研发提效。

讲项目时按这个顺序：

1. **业务链路**：扫码借伞、设备开锁、订单计费、归还识别、押金 / 活动 / 售后；
2. **业务痛点**：借伞失败、归还未结束、设备离线、退款争议、网点调度；
3. **AI 方案**：意图识别、RAG、工具调用、Agent Workflow、SSE；
4. **项目难点**：多源状态联合判断、规则更新、输出稳定、权限边界；
5. **项目亮点**：不是 ChatBot，而是结合订单 / 设备 / 网点 / SOP 的业务处理助手。

一句话总结：

> 我做的不是脱离业务的 AI Demo，而是把公共雨伞借还业务中的异常处理和客服运营流程，抽象成可检索、可调用、可解释、可管控的 AI 工作流。

OpenClaw 后台自动化一句话总结：

> 我把 OpenClaw 接入公共雨伞后台系统，把订单、设备、网点、售后、活动等能力封装成 CLI / Tool，再通过前端自动化操作台和数字人入口触发 Agent 工作流，实现客服运营流程自动化和研发代码提效。
